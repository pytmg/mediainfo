[project]
name = "pytmg-mediainfo"
version = "0.1.1"
description = "A Python wrapper around ffprobe"
requires-python = ">=3.10"
license = "GPL-3.0-only"
readme = "README.md"
authors = [
    { name = "pytmg" }
]